import { StyleSheet } from 'react-native';
export default styles = StyleSheet.create({
    cssTxt: {
        color: '#A4A4A4',
        fontSize: 14
    },
    cssPrice: {
        color: '#1C1C1C',
    }

});
